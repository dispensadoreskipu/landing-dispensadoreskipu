version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kipu-landing
    ports:
      - "4545:80"
    restart: unless-stopped
    networks:
      - kipu-network

networks:
  kipu-network:
    driver: bridge

